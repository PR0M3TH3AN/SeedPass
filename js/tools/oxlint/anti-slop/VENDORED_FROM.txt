Vendored from https://github.com/dmmulroy/anti-slop
commit: 6d538555cb151d4121ed51a27db81890eacf8ae9
vendored: 2026-08-20

This is a COPY, deliberately. The upstream project describes itself as
something to vendor and own rather than depend on, and its rules are
opinionated -- several of them are wrong for this repository and are not
enabled (see ../../oxlint.config.ts for which two are, and why).

Editing these files is expected. If you change a rule, say so here.
Upstream's own tests were dropped: we do not run them, and keeping tests we
never execute would be its own kind of slop.

Local changes since vendoring: none.
