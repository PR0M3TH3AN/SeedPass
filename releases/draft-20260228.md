# Weekly Changelog: 2026-02-21 to 2026-02-28

## Highlights
* Re-enabled skip test logic for pty/termios on Windows (`test_ai_tui_agent_harness`).
* Simplified GUI backend config handling by refactoring it to `constants.py`.
* Fixed arbitrary file overwrite vulnerability in backup processing.
* Reduced code complexity and redundancy across multiple entry management modules.
* Assorted test reliability fixes and CI updates.

## Changed
* Refactored `modify_entry` to reduce complexity by using helper methods (96afc4a)
* Refactored entry menus to use generic `MenuHandler.run_menu` (a1b459f)
* Refactored GUI backend config to `constants.py` (9e08e50)
* Refactored `handle_add_*` methods to remove duplication (4ebc14e)
* Refactored `Vault.load_index` into smaller private methods (e6ff619)
* Refactored `_enforce_complexity` in `password_generation.py` (a6856c1)
* Refactored `display_sensitive_entry_info` into `DisplayService` (40f37b3)
* Refactored `initialize_managers` in `PasswordManager` for better readability (24fa2e8)
* Refactored imghdr compatibility hack to a dedicated compat module (f9023e8)

## Fixed
* `test_ai_tui_agent_harness` on Windows platform skip handling (4b15596, a4fb4c2, b8da933, f7eb3a3, 596b491, ee7cde1)
* Removed unformatted `patch.py` from git history and repo (f39669a)
* Race condition in backup file permissions (bee0d62)
* Added pycryptodome dependency for PGP RSA features (1dc98a3)
* Prevented arbitrary file overwrite in `backup_parent_seed` (39f552d)
* Fixed legacy empty salt key derivation issues (b5a3215)
* Fixed index verification for TOTP in modify entry test (29b955c)
* Fixed macOS build script and updated `requirements.lock` (b61386b)

## Added
* Favicon and switched accent palette to green for landing page (e089574)
* Comprehensive unit tests for `generate_fingerprint` (6a921b8)
* Unit test for `StatsManager.reset` method (407dfee)
* Test to verify `atomic_write` cleanup on failure (566988b)
* Test to verify `verify_checksum` behavior when file is missing (00d19d9)

## Performance
* Offloaded blocking `import_vault` operations to thread (cf9d473)

## Potential breaking changes / migrations — needs review
* None explicitly detected in this timeframe.

## Missing metadata
* *PR and author metadata are unavailable in the current environment and omitted.*
