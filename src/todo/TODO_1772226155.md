## Constants Refactoring TODOs

1. `1000` (second in ms): Replaced with `SECOND_MS`
2. `60000` (minute in ms): Replaced with `MINUTE_MS`
3. `24 * 60 * 60 * 1000` (day in ms): Replaced with `DAY_MS`
4. `280` (max summary length): Replaced with `MAX_SUMMARY_LENGTH`
5. `0.25` (default importance): Replaced with `DEFAULT_IMPORTANCE_SCORE`
6. `0.35` (low importance threshold): Replaced with `LOW_IMPORTANCE_THRESHOLD`

Status: All complete. Constants extracted to `torch/src/services/memory/constants/`.
