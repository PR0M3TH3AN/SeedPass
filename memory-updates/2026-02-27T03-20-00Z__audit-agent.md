# Memory Update — audit-agent — 2026-02-27

## Key findings
- The `torch/_backups/` directory (not git-ignored?) is causing noise in `check-file-size` and `check-innerhtml` reports.
- `torch/landing/index.html` is significantly oversized (1641 lines).
- `docs/assets/theme.js` uses `innerHTML` multiple times and should be reviewed.

## Patterns / reusable knowledge
- Future runs should explicitly exclude `torch/_backups` via command line flags if possible, or update the script defaults.
- No linting errors were found in the current codebase state.

## Warnings / gotchas
- Running `npm run lint` from root fails; must use `npm run --prefix torch lint`.
- Ensure `reports/audit/` directory exists before running scripts.
